services:
  build:
    build:
      context: .
      target: builder
    volumes:
      - ./dist:/out
    entrypoint: ["/bin/sh", "-c"]
    command: ["cp /src/build/touch_ray_demo /src/build/webcam_preview /out/ && chmod +x /out/touch_ray_demo /out/webcam_preview"]
